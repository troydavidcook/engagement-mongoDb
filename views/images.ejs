<% include ./partials/header %>

    <div class="ui container main">
        <div class="ui container segment">
            <h3>Welcome to Kevin's proposal to Dallyn! Scroll down to see how it happened</h3>

            <% images.forEach((image) => { %>
                <a href="/images/<%= image._id %>"><img class="ui centered image bordered massive" src="<%= image.url %>" alt=""></a>
        <hr class="style-two">
            <% }); %>
        </div>
    </div>

<% include ./partials/footer %>